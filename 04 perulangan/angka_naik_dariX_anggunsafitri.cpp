#include <iostream>
using namespace std;

int main()
{
    int a, c;
    cin >> a >> c;
    if (a <= c)
    {
        for (int i = a; i <= c; i++)
        {
            cout << i << " ";
        }
    }
    else
    {
        for (int i = a; i <= c; i++)
        {
            cout << i << " ";
        }
    }
    cout << endl;
    return 0;
}